{
  "name": "equation_finder_bbn",
  "version": "0.0.1",
  "description": "This is the equation finder using the babun cli ",
  "main": "index.js",
  "scripts": {
    "watch": "cross-env NODE_ENV=development parcel index.html --no-hmr --open",
    "build": "cross-env NODE_ENV=production parcel build index.html --no-minify --public-url ./",
    "link-local": "yalc link",
    "postinstall": "yarn upgrade --pattern @tensorflow"
  },
  "author": "sonnyftw",
  "license": "ISC",
  "private": true,
  "engines": {
    "node": "10.13.0"
  },
  "dependencies": {
    "@tensorflow/tfjs": "^0.13.0",
    "vega-embed": "^3.2.0",
    "vega-lite": "^2.3.1",
    "yarn": "^1.12.3"
  },
  "devDependencies": {
    "babel-plugin-transform-runtime": "~6.23.0",
    "babel-polyfill": "~6.26.0",
    "babel-preset-env": "~1.6.1",
    "clang-format": "~1.2.2",
    "cross-env": "^5.2.0",
    "parcel-bundler": ">=1.10.0",
    "yalc": "~1.0.0-pre.22"
  }
}
